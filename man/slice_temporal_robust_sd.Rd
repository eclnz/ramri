% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array_functions.r
\name{slice_temporal_robust_sd}
\alias{slice_temporal_robust_sd}
\title{Calculate Robust Temporal Standard Deviation for a Slice}
\usage{
slice_temporal_robust_sd(x, na.rm = TRUE, mask = NULL, scale_factor = 1.4826)
}
\arguments{
\item{x}{A 3D array with dimensions (x, y, t), where t represents time.}

\item{na.rm}{A logical indicating whether NA values should be removed. Default is TRUE.}

\item{mask}{An optional binary mask of dimensions (x, y) to restrict calculations to specific regions. Default is NULL.}

\item{scale_factor}{Scaling factor to make MAD comparable to standard deviation. Default is 1.4826.}
}
\value{
A 2D array of shape (x, y) containing the robust standard deviation values.
}
\description{
Computes a robust estimate of standard deviation using median absolute deviation (MAD) across time.
}
\examples{
set.seed(123)
# Create a sample 3D slice with dimensions 10x10x20 (x, y, time)
slice <- array(rnorm(10 * 10 * 20), dim = c(10, 10, 20))
# Add some outliers
slice[5, 5, 10] <- 100  # Add an outlier
# Calculate robust standard deviation
robust_sd <- slice_temporal_robust_sd(slice)
}
